# users
User is a C++ user account management tool.


The first version is an object which loads JSON object with the usernames. For now that's the goal. 

The next step will be to encrypt the file. 

The next step will be to use sqlite to manage users data. 


